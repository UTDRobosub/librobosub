
#pragma once

#include "common.h"

#include <opencv2/opencv.hpp>

/*
template <class type>
union matrawval{
    type v;
    char bytes[sizeof(type)];
};

template <class type, int vals>
void MatToRaw(Mat*,int,int,char*);

template <class type, int vals>
void RawToMat(Mat*,int,int,char*);
*/
